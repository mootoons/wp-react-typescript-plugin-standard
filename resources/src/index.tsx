import * as React from 'react';
import { render } from '@wordpress/element';
import App from '@components/app';

// Render the App component into the DOM
render(<App />, document.getElementById('my-app-app'));
